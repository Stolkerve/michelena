[package]
name = "michelena"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
anyhow = "1.0"
sdl3 = { version = "0", features = ["build-from-source-static"] }

[build-dependencies]
gl_generator = "0.14.0"
cfg_aliases = "0.2.1"

